namespace Seeker.Components;

// TODO: See where this is used and probably remove it, or maybe use icon resources for enum values
public enum SharingIcons
{
    Off = 0,
    Error = 1,
    On = 2,
    CurrentlyParsing = 3,
    OffDueToNetwork = 4,
}
